name: Deploy Docker Image

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: RajBDevops/docker-image-reusable/.github/actions/docker-build-push@main

        with:
          image_name: rajbdocker/demo-ecom
          tag: latest
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
